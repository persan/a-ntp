library project NTP is

   for Source_Dirs use ("src");
   for Object_Dir use ".obj";
   for Library_Name use "a-ntp";
   for Library_Dir use "lib";
   package Compiler is
      for Switches ("ada") use ( "-gnatwa", "-gnatyaAbBcdfhiklnOprtux", "-gnatX");
   end Compiler;

end NTP;

